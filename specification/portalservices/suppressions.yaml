- tool: TypeSpecValidation
  paths:
    - Extension.Management/tspconfig.yaml
    - Dashboard.Management/tspconfig.yaml
  rules:
    - SdkTspConfigValidation
  sub-rules:
    - parameters.service-dir.default
    - options.@azure-tools/typespec-go.*
    - options.@azure-tools/typespec-java.*
    - options.@azure-tools/typespec-python.*
    - options.@azure-tools/typespec-ts.*
  reason: These two resources are not related to tenant settings and should not be validated by tenant settings-specific rules. Also remove specs containing incorrect or missing emitter options or parameters