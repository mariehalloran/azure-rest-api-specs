# AIGrounding Resource Provider

> see https://aka.ms/autorest

This is the AutoRest configuration file for the AIGrounding Resource Provider.

---

## Getting Started
To build the SDK for the AIGrounding RP, simply [Install AutoRest](https://aka.ms/autorest/install) and in this folder, run:

> `autorest`

To see additional help and options, run:

> `autorest --help`

---

## Configuration

### Basic Information
These are the global settings for the AIGrounding RP.

``` yaml
title: AIGroundingConfigurationClient
description: AIGrounding Configuration Client
openapi-type: arm
openapi-subtype: rpaas
tag: package-2026-06-01-preview
```

### Suppressions

``` yaml
directive:
  - suppress: RequiredPropertiesMissingInResourceModel
    from: aigrounding.json
    where: $.definitions.ResourceSkusResult
    reason: This is a list response envelope, not an ARM tracked resource.
  - suppress: RequiredPropertiesMissingInResourceModel
    from: aigrounding.json
    where: $.definitions.AIGroundingAccountEnumerateSkusResult
    reason: This is a list response envelope, not an ARM tracked resource.
  - suppress: RequiredPropertiesMissingInResourceModel
    from: aigrounding.json
    where: $.definitions.UsagesResult
    reason: This is a list response envelope, not an ARM tracked resource.
  - suppress: PatchBodyParametersSchema
    from: aigrounding.json
    reason: "Using ARM common-types Sku reference for PUT/PATCH symmetry. The sku property itself is optional on the PATCH body; required sku.name only applies if client opts to send sku."
```

### Tag: package-2026-06-01-preview

These settings apply only when `--tag=package-2026-06-01-preview` is specified on the command line.

``` yaml $(tag) == 'package-2026-06-01-preview'
input-file:
- preview/2026-06-01-preview/aigrounding.json
```

---

# Code Generation

## Swagger to SDK

This section describes what SDK should be generated by the automatic system.
This is not used by Autorest itself.

``` yaml $(swagger-to-sdk)
swagger-to-sdk:
  - repo: azure-sdk-for-net
  - repo: azure-sdk-for-python
  - repo: azure-sdk-for-java
  - repo: azure-sdk-for-go
  - repo: azure-sdk-for-js
  - repo: azure-sdk-for-node
  - repo: azure-sdk-for-ruby
    after_scripts:
      - bundle install && rake arm:regen_all_profiles['azure_mgmt_aigrounding']
```

## Multi-API/Profile support for AutoRest v3 generators

AutoRest V3 generators require the use of `--tag=all-api-versions` to select api files.

This block is updated by an automatic script. Edits may be lost!

``` yaml $(tag) == 'all-api-versions' /* autogenerated */
# include the azure profile definitions from the standard location
require: $(this-folder)/../../../../profiles/readme.md

# all the input files across all versions
input-file:
  - $(this-folder)/preview/2026-06-01-preview/aigrounding.json

```

If there are files that should not be in the `all-api-versions` set,
uncomment the  `exclude-file` section below and add the file paths.

``` yaml $(tag) == 'all-api-versions'
#exclude-file:
#  - $(this-folder)/Microsoft.Example/stable/2010-01-01/somefile.json
```
